version: "2"

linters:
  enable:
    - gofmt
    - goimports
    - govet
    - errcheck
    - staticcheck
  settings:
    goimports:
      local-prefixes: memodroid

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
