version: "2"

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - memodroid

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
