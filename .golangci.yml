version: "2"

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - memodroid

linters:
  enable:
    - govet
    - errcheck
    - staticcheck

issues:
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
